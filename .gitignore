.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Runtime state the library writes. A contributor running the quickstart inside
# the checkout would otherwise commit their own persona databases and their own
# (coarsened) coordinates.
*.db
.device_location.json

# go-public run state (see /public-repo skill)
public-repo/

# uv lockfile: not committed for a library; CI resolves from pyproject.toml
uv.lock
